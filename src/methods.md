🔥 Отлично, вот тебе чек-лист по методам коллекции в Laravel, когда нужно проверить существование/значение.

🔍 Проверка наличия значения
$collection->contains('id', $id);
// true/false, если в коллекции есть элемент с ключом 'id' равным $id


🔍 Проверка через callback
$collection->contains(function ($item) use ($id) {
    return $item->id === $id;
});


🔍 Проверка наличия ключа
$collection->has('key');
// true/false, есть ли такой ключ (для коллекций как массива)


🔍 Получить только нужные значения и проверить
$collection->pluck('id')->contains($id);
// достаём только id и проверяем


🔍 Фильтрация + проверка непустоты
$collection->where('id', $id)->isNotEmpty();
// true/false


🔍 Проверить, что все элементы соответствуют условию
$collection->every(fn($item) => $item->active === true);
// true, если все элементы "active"


🔍 Проверить, что хотя бы один соответствует условию
$collection->some(fn($item) => $item->age > 18);
// alias contains() с callback


✅ Итог:

быстрое наличие по ключу → contains('id', $id)

массив значений → pluck('id')->contains($id)

с фильтрацией → where('id', $id)->isNotEmpty()

сложные условия → contains(callback) или some(callback)


1. Eloquent Model — Базовые методы для запросов и модели

Eloquent — это адаптивный Query Builder плюс ORM, предоставляющий множество удобных методов. Сюда входит всё, что возвращает один или несколько объектов модели:

Создание / обновление:

create([...]), firstOrCreate([...]), firstOrNew([...]), updateOrCreate(...), fill([...])

laravel-docs.readthedocs.io

Удаление:

delete(), destroy([id])

laravel-docs.readthedocs.io

Обновление из базы:

fresh(), refresh()

laravel.com

Чтение:

all(), where()->get(), orderBy(), limit()/take(), а также весь функционал Query Builder

laravel.com

Работа с масштабом / агрегациями:

withCount(), withSum() — позволяют получить количество или суммы связанных моделей в том же запросе

Medium

Транзакционная защита:

lockForUpdate(), sharedLock() — позволяют блокировать ряды для одновременных транзакций

Medium


2. Еloquent Collection — методы для работы с множеством моделей

Если get() или all() вернули больше одной модели — это всегда Illuminate\Database\Eloquent\Collection. Он наследует методы коллекции Laravel, например:

contains(), map(), filter(), pluck(), where(), reject() и т.д.

laravel.com
docs.cornch.dev

И добавляет уникальные для Eloquent методы:

fresh(), load(), loadMissing(), modelKeys(), makeHidden(), makeVisible(), append(), toQuery(), unique()

laravel.com
docs.cornch.dev

Примеры:

append('team') — добавить аксессоры к каждому элементу

makeHidden('likes') — скрыть поле при сериализации

laravel.com


3. Laravel Support Collection — общий набор методов для работы с любыми коллекциями

Это Illuminate\Support\Collection. Он даёт огромный инструментарий для продвинутой работы с данными:

chunk(), average(), combine(), diff(), filter(), flatten(), flip(), mapWithKeys(), partition(), random(), shuffle(), unique(), и десятки других

blog.codewithdary.com
Backpack
saasykit.com

То, что вам точно пригодится:

filter(), map(), reduce() — для обработки данных;

pluck() — быстро выбрать нужные поля;

chunk() — обрабатывать большие коллекции без OOM;

keyBy() и mapWithKeys() — удобная структура для быстрых lookup;

firstWhere() — найти первый подходящий элемент по условию.